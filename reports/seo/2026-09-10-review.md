# Weekly SEO review — 2026-09-10

## 1. Headline

This week (2 Sep–8 Sep): **0 clicks / 20 impressions**, impressions down 68% vs the prior 7 days. Trailing 28 days: **8 clicks / 727 impressions**, both down sharply vs the previous 28 (clicks -74.2%, impressions -83.1%), avg position 22.1. The single most important fact: PR #31 (seo/crawlable-catalog) merged into `main` at **05:40 UTC today**, roughly two hours before this report collected at 07:48 UTC — `/skills` now has 48 `<a>` links and `/skills/page/2` returns HTTP 200 with 48 anchors, the first working crawl path into the catalog since the 15–16 Aug collapse. Google has not had time to act on it: URL Inspection still shows `/skills` as "Discovered - currently not indexed" and `/skills/page/2` as "URL is unknown to Google."

## 2. Improved

- `/skills` anchor count: **0 → 48** (baseline: zero `<a>` to any skill page after the 11 Aug pagination change).
- `/skills/page/2`: now **HTTP 200, 48 anchors** (previously did not exist as a crawlable URL).
- Sitemap: **621 URLs, 0 UUID URLs, 47 pagination pages** — back from the 29 Jul gate that cut ~1,500 URLs.
- Audit score **8.9/10, 0 high-severity failures**, 23/26 checks passing.
- Pages linked internally that return non-200: **0**.

## 3. Regressed / broken

- Weekly impressions have not moved: wk 2026-08-19 = 60, wk 08-26 = 63, wk 09-02 = 20 — flat-to-down, no bounce yet (expected — fix is hours old).
- `/skills` verdict: **Discovered - currently not indexed**, no recorded crawl date — Google hasn't revisited since the fix.
- `/skills/page/2` verdict: **URL is unknown to Google**.
- **235 pages** have ≤1 inbound link, almost all individual `/skills/*` pages — thin internal linking, likely capping crawl depth.
- **16 UUID URLs** still linked (e.g. `/skills/4ac5a28d-...`, `/a/77fdcc6f-...`), despite the Sep 7 slug-backfill fix — slug generation for these hasn't run or isn't complete.
- Audit flags: sitemap over the 600-URL "focused" threshold (621, medium), 4 titles over 65 chars, 13 descriptions over 165 chars (medium).
- Blog: **0 posts published in 7 days** despite 7/7 daily runs succeeding — see §6.

## 4. Catalog recovery

Weekly impressions since the 15 Aug collapse: wk 08-12 = 584 (pre-collapse tail), wk 08-19 = 60, wk 08-26 = 63, wk 09-02 = 20. **No recovery in the numbers yet.** Structurally, the fix is live: `/skills` anchor count = 48, `/skills/page/2` = HTTP 200 / 48 anchors. But both are still unindexed per URL Inspection (`/skills`: "Discovered - currently not indexed", no crawl date; `/skills/page/2`: "URL is unknown to Google"). Verdict: recovery has not started — the fix merged only ~2 hours before this report, so Google has had zero opportunity to recrawl. Watch item for next week: if `/skills` still shows no crawl date and impressions for wk 09-09 stay flat, request indexing manually rather than waiting.

## 5. Original pages

- `/templates`: 0 clicks, not present in either 7d or 28d section breakdown — **zero impressions**, not just zero clicks.
- `/automate`: 0 clicks / 3 impressions (28d only).
- `/mcp`: 0 clicks / 3 impressions (28d only).
- `/blog`: not present in 7d or 28d breakdowns — **zero**.
- `/bundles`, `/collections`: not present in either breakdown — **zero**; `/collections` also verdicts "Discovered - currently not indexed."

Every original-content section remains at zero clicks, consistent with the 7 Sep baseline. Combined 28-day impressions across automate + mcp + build-for-me + submit + about total 11.

## 6. Blog engine

Published in the last 7 days: **0 — stopped.** Last published: `n8n-vs-openclaw`, 2026-09-02 (8 days ago). Held (6): shopify-review-automation 5.6, cold-email-automation-claude 6, cold-email-n8n-guide 6.5, blog-to-linkedin-x (unscored), content-repurposer-blog-linkedin-x 6, auto-reply-shopify-reviews-n8n 7. In flight: meeting-notes-n8n (drafting), shopify-review-reply-claude (briefed). Daily runs: 7/7 "success" — the pipeline runs cleanly but produces nothing publishable. **Groq at gate 7 is not producing**: 5 of 6 recent drafts scored below the gate. Gemini was merged as the primary writer today (PR #33, 05:49 UTC) specifically to fix this, but this report's own provider check reads `{"groq":true,"gemini":false}` — Gemini isn't active yet in production, most likely a missing `GEMINI_API_KEY`.

## 7. Toward 1,000/day

Current run-rate is ~0.3 clicks/day against a 1,000/day target — nowhere close, as expected this early. Of the arithmetic (40 posts / 150 mid pages / 600 catalog pages), **0 of the 150 mid pages and 0 of the 40 editorial posts have a single click**, and the 600-page catalog only regained a crawl path today. The one lever that moves next week's number most: **get Google to actually crawl and index `/skills` and `/skills/page/2`** — nothing else compounds until the 510-URL catalog is indexed.

1. URL Inspection → Request Indexing for `/skills` and `/skills/page/2`.
2. Confirm `GEMINI_API_KEY` is set in production so PR #33's writer actually engages (currently `gemini: false`).
3. Add a "related skills" internal-linking module to cut into the 235 pages with ≤1 inbound link.
4. Finish the UUID→slug backfill for the 16 remaining raw-UUID URLs under `/skills` and `/a`.
5. Add outbound links from `/skills/*` pages into relevant `/templates` and `/automate` pages — those sections have zero impressions and no path in from the catalog.

## 8. Do not touch

- Sitemap size (621 URLs, flagged "not focused") — leave it; the catalog just regained its crawl path today and Google hasn't attempted most of it yet.
- The blog judge gate (7) and disabled revision — working as designed per the 7 Sep decision; the real fix is Gemini activation (task 2), not loosening the gate.
- `/templates/cold-email-personalizer` and `/automate/triage-your-inbox` — PASS/indexed with old crawl dates (Jul 23/25); healthy, just no query volume yet, don't resubmit.
- Title/description length flags (4 titles, 13 descriptions) — cosmetic, won't move rankings this week; lower priority than the indexation blocker above.
