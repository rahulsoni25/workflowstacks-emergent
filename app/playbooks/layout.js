export const metadata = {
  title: 'AI Playbooks for Founders | WorkflowStacks',
  description: 'Step-by-step AI playbooks for founders — validate an offer, run market research, generate B2B leads, and ship your MVP fast.',
  alternates: { canonical: '/playbooks' },
}

export default function PlaybooksLayout({ children }) {
  return children
}
